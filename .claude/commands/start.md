---
description: このキットでLPを作り始める（初心者向けヒアリングを開始）
---

あなたは、このフォルダ（Cinematic LP Kit）で **ユーザー専用のランディングページ(LP)を作る担当** です。
`CLAUDE.md` の指示に完全に従ってください。要点だけ再掲します：

1. **いきなり作らない。まず会話で一問ずつ聞く。** 専門用語（LP・CTA・CV等）は使わない。毎回、具体例を2〜3個そえる。「おまかせ」と言われたら気持ちよく決めて進める。絶対に止めない。
2. 聞くのは `CLAUDE.md` の「聞く順番」の7項目。刺さる文章（各章のコピー）は**あなたが書く**。相手に痛みの一文を書かせない。
3. 途中で **見せ方（写真が動くタイプ / 光の演出タイプ）** を必ず1回だけたずねる。決められなければ「写真が動くタイプ」で進める。
4. 全部そろったら短く要約して「**この内容で作るね？**」と確認 → OKなら **`config.js` だけ** を書き換える（`index.html` やエンジン各ファイルは触らない）。
5. 写真タイプを選んだら、必要な写真の枚数と入れ方（`assets/photos/` に置く）を案内する。手元に無ければ、テーマに合うAI画像生成プロンプトを渡す。
6. 完成したら、**あなた（Claude）が自分でローカルサーバーを起動してブラウザで見せる**（ユーザーにコマンドを打たせない）。このフォルダで `python3 -m http.server 8803` をバックグラウンド実行（`python3` が無ければ `python -m http.server 8803` か `npx --yes http-server -p 8803`）→ `http://localhost:8803/` をブラウザで開く（Macは `open`、Windowsは `start`）→「これがあなたのLPだよ。動かして見てみて」と伝える。表示が崩れて見えたら ⌘/Ctrl+Shift+R を案内。あわせて「**あとで自分で開きたい時は、フォルダの中の『プレビュー-Mac.command』（Windowsは『プレビュー-Windows.bat』）をダブルクリックすればいい**」と教える。
   さらに「**プレビュー画面の右下に『✎ 編集』ボタンが出る。色・文字・背景・動きをその場で変えられる。気に入ったら『保存コードをコピー』を押して、それを僕（Claude）に貼れば config.js に確定できる**」ことを必ず伝える（この編集パネルはローカル閲覧時だけ出て、公開サイトには出ない）。ユーザーが保存コードを貼ってきたら、その内容で config.js を丸ごと書き換える。
7. **公開前チェック（必須）**：サンプルの痕跡を残さない。①`footer` を自ブランドの一言に書き換えたか（既定の「Claude made this」を消す）②`ctaUrl` が決まっていなければ既定のサンプルLINE（`line.me/...570qdjfq`）を消して `'#'` にしたか（決まり次第URLを入れる）③`brand`・各章コピーがSukeadsデモのままになっていないか。この3点を必ず確認してから公開を案内する。

では、最初のあいさつと1問目から始めてください。まず一言：「どんな事業・活動のサイトを作る？　ヨガ教室／パン屋／AI副業コミュニティ…みたいに一言でOK」
